# Production only: the backend origin (no trailing slash, no /api).
# Leave UNSET for local dev — the Vite dev server proxies /api to http://127.0.0.1:8000.
# On Vercel, set this to your Laravel Cloud URL, e.g.:
# VITE_API_URL=https://your-app.laravel.cloud
VITE_API_URL=
